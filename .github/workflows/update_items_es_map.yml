name: Update items ES map (monthly)

on:
  workflow_dispatch:
  schedule:
    # 1 vez al mes: día 1 a las 03:20 UTC
    - cron: "20 3 1 * *"

permissions:
  contents: write

jobs:
  update_items:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Run updater
        env:
          ITEMS_POOL: "5"
        run: node scripts/update_items_es_map.js

      - name: Commit & push if changed
        run: |
          if git status --porcelain | grep .; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add -A
            git commit -m "chore(items): update es map"
            git push
          else
            echo "No changes."
          fi